import matching_mechanism

#######################
# Program entry point #
#######################
if __name__ == "__main__":
    # Define the marching mechanism to use and pass the disaster region (as a
    # string)
    # e.g.
    # matching_mechanism.region_to_region_recovery("europe-west3", "europe-west4")
    # matching_mechanism.region_to_many_recovery("europe-west3")
    print(
        "TODO: Specify the matching mechanism disaster recovery strategy and the disaster region (and possibly the recovery region)"
    )
